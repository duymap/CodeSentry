# Code Diff Analyzer Configuration
# Copy this file to .env and update with your repository path

# Path to the git repository to analyze
REPO_PATH=/path/to/your/repository

# Optional: Use local LLM via Ollama instead of Claude Code CLI
# If not set, Claude Code CLI will be used by default
# Example: LOCAL_LLM=mistral
# Example: LOCAL_LLM=llama2
# Example: LOCAL_LLM=codellama
# LOCAL_LLM=
